// Shared, user-friendly guidance shown when a permission has been blocked.
// These appear only in response to an explicit user action (tapping a feature
// or retry), never on app launch.

export const LOCATION_BLOCKED_HELP =
  "Location is blocked. To turn it on: tap the lock or ⓘ icon next to the address bar → Permissions → allow Location, then try again.";

export const MIC_BLOCKED_HELP =
  "Microphone is blocked. To turn it on: tap the lock or ⓘ icon next to the address bar → Permissions → allow Microphone, then tap the mic again.";
